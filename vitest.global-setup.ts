// This runs ONCE in the main process before any tests
// Import the compiler to make it globally available
import '@angular/compiler';

export default async function () {
  // Compiler is now loaded globally
}
